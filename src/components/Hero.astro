---
---
<section
  class="hero-institucional"
  style="background-image:
    radial-gradient(circle at top, rgba(255,255,255,0.12), transparent 55%),
    linear-gradient(180deg, rgba(8,63,115,0.85), rgba(10,92,160,0.92)),
    url('/img/hero-natal.png');"
>
  <div class="hero-overlay"></div>

  <div class="hero-content animate-fade-up">
    <!-- LOGO -->
    <div class="hero-logo">
      <img
        src="/img/logo-burareiro.png"
        alt="Escola Roberto Turbay"
      />
    </div>

    <!-- TEXTO -->
    <h1 class="hero-title">
      <span class="thin">Bem-vindo à</span>
      <span class="strong highlight">Escola Roberto Turbay</span>
    </h1>

    <p class="hero-subtitle">
      Projeto Burareiro de Educação Integral
    </p>
  </div>
</section>

<style>
.hero-institucional {
  position: relative;
  min-height: 70vh;
  display: flex;
  align-items: center;
  justify-content: center;

  /* imagem natalina + identidade azul */
  background-repeat: no-repeat;
  background-position: center;
  background-size: cover;

  overflow: hidden;
}

/* brilho sutil */
.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    120deg,
    rgba(255,255,255,0.06),
    transparent 40%,
    rgba(0,0,0,0.25)
  );
  pointer-events: none;
}

.hero-content {
  position: relative;
  text-align: center;
  color: white;
  padding: 2rem 1rem;
  max-width: 900px; /* 🔒 mantém o “quadrado” grande */
}

/* LOGO */
.hero-logo {
  margin-bottom: 1.5rem;
}

.hero-logo img {
  width: 130px;
  height: auto;
  filter: drop-shadow(0 10px 25px rgba(0,0,0,0.35));
}

/* TÍTULO */
.hero-title {
  line-height: 1.15;
  margin: 0;
}

.hero-title .thin {
  display: block;
  font-weight: 400;
  font-size: clamp(1.6rem, 3vw, 2.4rem);
  letter-spacing: 0.04em;
  opacity: 0.95;
}

.hero-title .strong {
  display: block;
  font-weight: 900;
  font-size: clamp(2.6rem, 6vw, 4.6rem);
  letter-spacing: -0.02em;
  margin-top: 0.2rem;
}

/* PARTE AMARELA (como você gostou) */
.hero-title .highlight {
  color: var(--accent);
}

/* SUBTÍTULO */
.hero-subtitle {
  margin-top: 1rem;
  font-size: 1.15rem;
  color: rgba(255,255,255,0.9);
  max-width: 720px;
  margin-left: auto;
  margin-right: auto;
}

/* ANIMAÇÃO */
@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-up {
  animation: fadeUp 0.9s ease-out both;
}
</style>
